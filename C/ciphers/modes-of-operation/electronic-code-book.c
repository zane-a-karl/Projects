#include <stdio.h>
#include <stdlib.h>
#include <string.h>

/**
 * TODO for next time:
 *  - make the blocks nodes in a linked list that holds a block_len bit-string
 *  - have a utility function that converts c-strings to bits
 *  - write note comments for ecb to remind you what ecb is (maybe include diagrams)
 */
int *ecb(int *ptxt,
	 int *key,
	 int block_len,
	 int ptxt_len) {

  int *ctxt = (int *)calloc(len, sizeof *ctxt);
  
  
  return ctxt;
}
